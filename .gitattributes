# Normalizar saltos de línea a LF en el repositorio
* text=auto eol=lf

# Scripts de shell — SIEMPRE LF (nunca CRLF, rompe en Linux)
*.sh text eol=lf

# Archivos binarios — sin conversión
*.png binary
*.jpg binary
*.ico binary
*.wasm binary
